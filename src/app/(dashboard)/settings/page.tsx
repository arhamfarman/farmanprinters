/**
 * Placeholder landing spot for the Settings nav item — press profile
 * (letterhead fields used by the PDF templates), user management, and
 * category/service-item pricing all belong here. Left as a stub rather
 * than guessed at, since it wasn't part of the deliverable list yet.
 */
export default function SettingsPage() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">Settings</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Press profile, staff accounts, and catalog/pricing management will live here.
      </p>
    </div>
  );
}
